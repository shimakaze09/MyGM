CMAKE_MINIMUM_REQUIRED(VERSION 3.20 FATAL_ERROR)

PROJECT(MyGM VERSION 0.6.7 LANGUAGES CXX)
MESSAGE(STATUS "Project: ${PROJECT_NAME} ${PROJECT_VERSION}")

INCLUDE(cmake/InitMyCMake.cmake)
INIT_MYCMAKE()

INIT_PROJECT()

SET(MY_USE_SIMD TRUE CACHE BOOL "use simd")
SET(MY_USE_SSE_4_1 TRUE CACHE BOOL "use sse 4.1")

ADD_DEP(MyTemplate 0.4.6)

ADD_SUB_DIRS_REC(include)
ADD_SUB_DIRS_REC(src)

EXPORT_TARGETS(DIRECTORIES "include;vs")